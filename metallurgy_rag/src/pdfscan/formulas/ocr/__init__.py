"""Повторное распознавание формул по рамкам из первого прохода.

Ставится в отдельное окружение: paddle несовместим с тем стеком, на котором
работает разбор PDF. Поэтому модуль не импортирует ничего тяжёлого из остального
проекта — только стандартную библиотеку, ``pypdfium2`` и ``Pillow``.
"""
